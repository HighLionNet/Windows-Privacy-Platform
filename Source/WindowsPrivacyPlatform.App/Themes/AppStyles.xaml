<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- Windows Light defaults. ThemeManager replaces these tokens live for the other complete themes. -->
  <Color x:Key="BgWindow">#C8E2F2</Color>
  <Color x:Key="BgSidebar">#061B35</Color>
  <Color x:Key="BgContent">#DFF1FA</Color>
  <Color x:Key="BgCard">#F2FAFE</Color>
  <Color x:Key="BgHover">#C4E7F6</Color>
  <Color x:Key="BgSelected">#9FD8EF</Color>
  <Color x:Key="BgSelectedBorder">#006BA8</Color>
  <Color x:Key="BgHeader">#C5E7F6</Color>
  <Color x:Key="BgAltRow">#D5EDF8</Color>
  <Color x:Key="BgMenu">#CBE8F6</Color>
  <Color x:Key="BorderSubtle">#8FC5DC</Color>
  <Color x:Key="BorderStrong">#4C91AF</Color>
  <Color x:Key="BorderRule">#145E7E</Color>
  <Color x:Key="TextPrimary">#062538</Color>
  <Color x:Key="TextSecondary">#214D61</Color>
  <Color x:Key="TextMuted">#476B7B</Color>
  <Color x:Key="Accent">#006BA8</Color>
  <Color x:Key="AccentHover">#005484</Color>
  <Color x:Key="AccentCyan">#006F89</Color>
  <Color x:Key="AccentSoft">#AEE1F2</Color>
  <Color x:Key="SidebarText">#F5FBFF</Color>
  <Color x:Key="SidebarMuted">#B3D8E8</Color>
  <Color x:Key="SidebarHover">#0D3A5D</Color>
  <Color x:Key="SidebarSelected">#0D5274</Color>
  <Color x:Key="Warning">#A35500</Color>
  <Color x:Key="WarningSoft">#FFE1A3</Color>
  <Color x:Key="Error">#C32F46</Color>
  <Color x:Key="ErrorHover">#9D1F34</Color>
  <Color x:Key="Success">#007A62</Color>
  <Color x:Key="SuccessHover">#005F4D</Color>
  <Color x:Key="SuccessSoft">#BDECDC</Color>
  <Color x:Key="Unknown">#526D7A</Color>
  <Color x:Key="UnknownSoft">#D6E6EC</Color>
  <Color x:Key="Conflict">#D04432</Color>
  <Color x:Key="ConflictSoft">#F6CDC5</Color>

  <!-- Domain identity accents -->
  <Color x:Key="DomainPrivacy">#4DDCF4</Color>
  <Color x:Key="DomainSecurity">#FF8D86</Color>
  <Color x:Key="DomainWindows">#52DDA7</Color>
  <Color x:Key="DomainApps">#C5AEFF</Color>
  <Color x:Key="DomainKnowledge">#FFD56F</Color>

  <SolidColorBrush x:Key="BrushBgWindow" Color="{DynamicResource BgWindow}" />
  <SolidColorBrush x:Key="BrushBgSidebar" Color="{DynamicResource BgSidebar}" />
  <SolidColorBrush x:Key="BrushBgContent" Color="{DynamicResource BgContent}" />
  <SolidColorBrush x:Key="BrushBgCard" Color="{DynamicResource BgCard}" />
  <SolidColorBrush x:Key="BrushBgHover" Color="{DynamicResource BgHover}" />
  <SolidColorBrush x:Key="BrushBgSelected" Color="{DynamicResource BgSelected}" />
  <SolidColorBrush x:Key="BrushBgHeader" Color="{DynamicResource BgHeader}" />
  <SolidColorBrush x:Key="BrushBgAltRow" Color="{DynamicResource BgAltRow}" />
  <SolidColorBrush x:Key="BrushBgMenu" Color="{DynamicResource BgMenu}" />
  <SolidColorBrush x:Key="BrushBorder" Color="{DynamicResource BorderSubtle}" />
  <SolidColorBrush x:Key="BrushBorderStrong" Color="{DynamicResource BorderStrong}" />
  <SolidColorBrush x:Key="BrushBorderRule" Color="{DynamicResource BorderRule}" />
  <SolidColorBrush x:Key="BrushTextPrimary" Color="{DynamicResource TextPrimary}" />
  <SolidColorBrush x:Key="BrushTextSecondary" Color="{DynamicResource TextSecondary}" />
  <SolidColorBrush x:Key="BrushTextMuted" Color="{DynamicResource TextMuted}" />
  <SolidColorBrush x:Key="BrushAccent" Color="{DynamicResource Accent}" />
  <SolidColorBrush x:Key="BrushAccentHover" Color="{DynamicResource AccentHover}" />
  <SolidColorBrush x:Key="BrushAccentCyan" Color="{DynamicResource AccentCyan}" />
  <SolidColorBrush x:Key="BrushAccentSoft" Color="{DynamicResource AccentSoft}" />
  <SolidColorBrush x:Key="BrushSidebarText" Color="{DynamicResource SidebarText}" />
  <SolidColorBrush x:Key="BrushSidebarMuted" Color="{DynamicResource SidebarMuted}" />
  <SolidColorBrush x:Key="BrushSidebarHover" Color="{DynamicResource SidebarHover}" />
  <SolidColorBrush x:Key="BrushSidebarSelected" Color="{DynamicResource SidebarSelected}" />
  <SolidColorBrush x:Key="BrushWarning" Color="{DynamicResource Warning}" />
  <SolidColorBrush x:Key="BrushWarningSoft" Color="{DynamicResource WarningSoft}" />
  <SolidColorBrush x:Key="BrushError" Color="{DynamicResource Error}" />
  <SolidColorBrush x:Key="BrushErrorHover" Color="{DynamicResource ErrorHover}" />
  <SolidColorBrush x:Key="BrushSuccess" Color="{DynamicResource Success}" />
  <SolidColorBrush x:Key="BrushSuccessHover" Color="{DynamicResource SuccessHover}" />
  <SolidColorBrush x:Key="BrushSuccessSoft" Color="{DynamicResource SuccessSoft}" />
  <SolidColorBrush x:Key="BrushUnknown" Color="{DynamicResource Unknown}" />
  <SolidColorBrush x:Key="BrushUnknownSoft" Color="{DynamicResource UnknownSoft}" />
  <SolidColorBrush x:Key="BrushConflict" Color="{DynamicResource Conflict}" />
  <SolidColorBrush x:Key="BrushConflictSoft" Color="{DynamicResource ConflictSoft}" />
  <SolidColorBrush x:Key="BrushDomainPrivacy" Color="{DynamicResource DomainPrivacy}" />
  <SolidColorBrush x:Key="BrushDomainSecurity" Color="{DynamicResource DomainSecurity}" />
  <SolidColorBrush x:Key="BrushDomainWindows" Color="{DynamicResource DomainWindows}" />
  <SolidColorBrush x:Key="BrushDomainApps" Color="{DynamicResource DomainApps}" />
  <SolidColorBrush x:Key="BrushDomainKnowledge" Color="{DynamicResource DomainKnowledge}" />

  <LinearGradientBrush x:Key="BrushSidebarChrome" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="{DynamicResource BgSidebar}" Offset="0" />
    <GradientStop Color="{DynamicResource SidebarHover}" Offset="1" />
  </LinearGradientBrush>
  <LinearGradientBrush x:Key="BrushHeaderChrome" StartPoint="0,0" EndPoint="1,0">
    <GradientStop Color="{DynamicResource BgHeader}" Offset="0" />
    <GradientStop Color="{DynamicResource BgSelected}" Offset="1" />
  </LinearGradientBrush>
  <LinearGradientBrush x:Key="BrushActionGradient" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="{DynamicResource Accent}" Offset="0" />
    <GradientStop Color="{DynamicResource Accent}" Offset="0.62" />
    <GradientStop Color="{DynamicResource AccentCyan}" Offset="1" />
  </LinearGradientBrush>

  <Style TargetType="Window">
    <Setter Property="Background" Value="{DynamicResource BrushBgWindow}" />
    <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
    <Setter Property="FontSize" Value="12.5" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
  </Style>

  <Style TargetType="UserControl">
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
    <Setter Property="FontSize" Value="12.5" />
  </Style>

  <Style TargetType="Label">
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
  </Style>

  <Style TargetType="TextBlock">
    <Setter Property="Foreground"
            Value="{Binding Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}" />
    <Style.Triggers>
      <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}" Value="{x:Null}">
        <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <Style TargetType="CheckBox">
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
  </Style>

  <Style TargetType="Menu">
    <Setter Property="Background" Value="{DynamicResource BrushBgMenu}" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="FontFamily" Value="Segoe UI" />
    <Setter Property="FontSize" Value="12.5" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Padding" Value="4,0" />
  </Style>

  <Style TargetType="MenuItem">
    <Setter Property="Background" Value="{DynamicResource BrushBgMenu}" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="Padding" Value="11,5" />
  </Style>

  <Style TargetType="Separator">
    <Setter Property="Background" Value="{DynamicResource BrushBorderStrong}" />
    <Setter Property="Margin" Value="0,2" />
  </Style>

  <Style x:Key="SidebarButton" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="Padding" Value="15,7,10,7" />
    <Setter Property="Margin" Value="0,0,8,1" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Foreground" Value="{DynamicResource BrushSidebarText}" />
    <Setter Property="FontSize" Value="12.5" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="0,4,4,0"
                  BorderThickness="3,0,0,0" BorderBrush="Transparent"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BrushSidebarHover}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BrushSidebarHover}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.45" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SidebarButtonSelected" TargetType="Button" BasedOn="{StaticResource SidebarButton}">
    <Setter Property="Background" Value="{DynamicResource BrushSidebarSelected}" />
    <Setter Property="Foreground" Value="White" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="0,4,4,0"
                  BorderThickness="3,0,0,0" BorderBrush="{DynamicResource BrushAccent}"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BrushSidebarSelected}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SidebarGroupLabel" TargetType="TextBlock">
    <Setter Property="FontSize" Value="10" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="Foreground" Value="{DynamicResource BrushSidebarMuted}" />
    <Setter Property="Margin" Value="12,14,8,4" />
    <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
  </Style>

  <Style x:Key="SectionRule" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource BrushBorderRule}" />
    <Setter Property="Height" Value="1" />
    <Setter Property="Margin" Value="0,12,0,12" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
  </Style>

  <Style x:Key="HeaderRule" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource BrushActionGradient}" />
    <Setter Property="Height" Value="2" />
    <Setter Property="Margin" Value="0,6,0,14" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
  </Style>

  <Style x:Key="PropertySheet" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource BrushBgCard}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushBorderStrong}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Margin" Value="0,0,0,12" />
    <Setter Property="CornerRadius" Value="4" />
  </Style>

  <Style x:Key="PropertySheetHeader" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource BrushBgHeader}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushBorderStrong}" />
    <Setter Property="BorderThickness" Value="0,0,0,1" />
    <Setter Property="Padding" Value="12,6" />
  </Style>

  <Style x:Key="PropertyGroup" TargetType="Border">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Margin" Value="0,0,0,4" />
  </Style>

  <Style x:Key="Card" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource BrushBgCard}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushBorderStrong}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="12,10" />
    <Setter Property="Margin" Value="0,0,0,12" />
    <Setter Property="CornerRadius" Value="4" />
  </Style>

  <Style x:Key="CardAccent" TargetType="Border" BasedOn="{StaticResource Card}">
    <Setter Property="BorderThickness" Value="3,1,1,1" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
  </Style>

  <Style x:Key="CardConflict" TargetType="Border" BasedOn="{StaticResource Card}">
    <Setter Property="BorderThickness" Value="3,1,1,1" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushConflict}" />
    <Setter Property="Background" Value="{DynamicResource BrushConflictSoft}" />
  </Style>

  <Style x:Key="ListRow" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource BrushBgCard}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}" />
    <Setter Property="BorderThickness" Value="0,0,0,1" />
    <Setter Property="Padding" Value="12,8" />
    <Setter Property="Margin" Value="0" />
    <Setter Property="Cursor" Value="Hand" />
  </Style>

  <Style x:Key="ListRowConflict" TargetType="Border" BasedOn="{StaticResource ListRow}">
    <Setter Property="BorderThickness" Value="3,0,0,1" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushConflict}" />
    <Setter Property="Background" Value="{DynamicResource BrushConflictSoft}" />
  </Style>

  <Style x:Key="ResultsDivider" TargetType="Grid">
    <Setter Property="Height" Value="3" />
    <Setter Property="Margin" Value="0,1,0,12" />
    <Setter Property="Background" Value="{DynamicResource BrushBorderStrong}" />
  </Style>

  <Style TargetType="TextBox">
    <Setter Property="Background" Value="{DynamicResource BrushBgCard}" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushBorderStrong}" />
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="MinHeight" Value="34" />
    <Setter Property="Padding" Value="10,5" />
    <Setter Property="SelectionBrush" Value="{DynamicResource BrushAccentSoft}" />
    <Style.Triggers>
      <Trigger Property="IsKeyboardFocused" Value="True">
        <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style TargetType="ComboBox">
    <Setter Property="Background" Value="{DynamicResource BrushAccentSoft}" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="MinHeight" Value="34" />
    <Setter Property="Padding" Value="10,5,48,5" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="42"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="ComboBorder" Grid.ColumnSpan="2" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="3"/>
            <ContentPresenter Grid.Column="0" Margin="10,5,8,5" VerticalAlignment="Center" HorizontalAlignment="Left"
                              IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>
            <ToggleButton Grid.ColumnSpan="2" x:Name="DropDownToggle" Focusable="False" ClickMode="Press"
                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                          Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
              <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                  <Border x:Name="ToggleBorder" Background="Transparent" BorderBrush="Transparent"
                          BorderThickness="0" CornerRadius="3">
                    <Path Data="M 0 0 L 4 4 L 8 0" Stroke="{DynamicResource BrushTextPrimary}"
                          StrokeThickness="1.5" HorizontalAlignment="Right" Margin="0,0,16,0" VerticalAlignment="Center"/>
                  </Border>
                  <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ToggleBorder" Property="Background" Value="{DynamicResource BrushBgHover}"/></Trigger>
                  </ControlTemplate.Triggers>
                </ControlTemplate>
              </ToggleButton.Template>
            </ToggleButton>
            <Popup Grid.ColumnSpan="2" x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                   PopupAnimation="Fade" Focusable="False">
              <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                      MaxHeight="320" Background="{DynamicResource BrushBgHeader}" BorderBrush="{DynamicResource BrushAccent}"
                      BorderThickness="1" CornerRadius="3">
                <ScrollViewer SnapsToDevicePixels="True"><ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/></ScrollViewer>
              </Border>
            </Popup>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.55"/></Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
              <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
              <Setter TargetName="DropDownToggle" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ComboBoxItem">
    <Setter Property="Background" Value="{DynamicResource BrushBgHeader}" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="Padding" Value="8,5" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Style.Triggers>
      <Trigger Property="IsHighlighted" Value="True"><Setter Property="Background" Value="{DynamicResource BrushBgHover}" /></Trigger>
      <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource BrushAccent}" /><Setter Property="Foreground" Value="White" /></Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="ListRowButton" TargetType="Button">
    <Setter Property="Background" Value="{DynamicResource BrushBgCard}" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}" />
    <Setter Property="BorderThickness" Value="0,0,0,1" />
    <Setter Property="Padding" Value="12,8" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              TextElement.Foreground="{TemplateBinding Foreground}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BrushBgHover}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
              <Setter TargetName="Bd" Property="BorderThickness" Value="3,0,0,1" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ListRowButtonConflict" TargetType="Button" BasedOn="{StaticResource ListRowButton}">
    <Setter Property="Background" Value="{DynamicResource BrushConflictSoft}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushConflict}" />
    <Setter Property="BorderThickness" Value="3,0,0,1" />
  </Style>

  <Style x:Key="ListHeader" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource BrushBgHeader}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushBorderStrong}" />
    <Setter Property="BorderThickness" Value="0,0,0,1" />
    <Setter Property="Padding" Value="12,6" />
  </Style>

  <Style x:Key="ListContainer" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource BrushBgCard}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushBorderStrong}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Margin" Value="0,0,0,12" />
    <Setter Property="CornerRadius" Value="4" />
  </Style>

  <Style x:Key="FilterBar" TargetType="Border" BasedOn="{StaticResource ListContainer}">
    <Setter Property="Background" Value="{DynamicResource BrushBgHeader}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
    <Setter Property="BorderThickness" Value="4,1,1,1" />
    <Setter Property="Padding" Value="14,12" />
    <Setter Property="Margin" Value="0,0,0,10" />
  </Style>

  <Style x:Key="SectionTitle" TargetType="TextBlock">
    <Setter Property="FontSize" Value="24" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="Margin" Value="0,0,0,2" />
  </Style>

  <Style x:Key="SectionSubtitle" TargetType="TextBlock">
    <Setter Property="FontSize" Value="12.5" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextSecondary}" />
    <Setter Property="Margin" Value="0,2,0,0" />
    <Setter Property="TextWrapping" Value="Wrap" />
  </Style>

  <Style x:Key="FilterLabel" TargetType="TextBlock">
    <Setter Property="FontSize" Value="11.5" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="Foreground" Value="{DynamicResource BrushAccent}" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Margin" Value="0,0,8,0" />
  </Style>

  <Style x:Key="GroupHeader" TargetType="TextBlock">
    <Setter Property="FontSize" Value="13" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextSecondary}" />
    <Setter Property="Margin" Value="0,14,0,6" />
  </Style>

  <Style x:Key="LabelMuted" TargetType="TextBlock">
    <Setter Property="FontSize" Value="11" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextMuted}" />
  </Style>

  <Style x:Key="ValueText" TargetType="TextBlock">
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="LineHeight" Value="19" />
  </Style>

  <Style x:Key="PrimaryValue" TargetType="TextBlock">
    <Setter Property="FontSize" Value="15" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="TextWrapping" Value="Wrap" />
  </Style>

  <Style x:Key="MetaText" TargetType="TextBlock">
    <Setter Property="FontSize" Value="11" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextMuted}" />
  </Style>

  <Style x:Key="PropLabel" TargetType="TextBlock">
    <Setter Property="FontSize" Value="12" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{DynamicResource BrushAccent}" />
    <Setter Property="VerticalAlignment" Value="Top" />
    <Setter Property="Margin" Value="0,4,16,4" />
  </Style>

  <Style x:Key="PropValue" TargetType="TextBlock">
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="VerticalAlignment" Value="Top" />
    <Setter Property="Margin" Value="0,4,0,4" />
  </Style>

  <Style x:Key="SheetLabel" TargetType="TextBlock">
    <Setter Property="FontSize" Value="12" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextSecondary}" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Margin" Value="12,6,10,6" />
  </Style>

  <Style x:Key="SheetValue" TargetType="TextBlock">
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Margin" Value="0,6,12,6" />
  </Style>

  <Style x:Key="Badge" TargetType="Border">
    <Setter Property="CornerRadius" Value="10" />
    <Setter Property="Padding" Value="7,2" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Background" Value="{DynamicResource BrushAccentSoft}" />
  </Style>

  <Style x:Key="BadgeSuccess" TargetType="Border" BasedOn="{StaticResource Badge}">
    <Setter Property="Background" Value="{DynamicResource BrushSuccessSoft}" />
  </Style>

  <Style x:Key="BadgeWarning" TargetType="Border" BasedOn="{StaticResource Badge}">
    <Setter Property="Background" Value="{DynamicResource BrushWarningSoft}" />
  </Style>

  <Style x:Key="BadgeConflict" TargetType="Border" BasedOn="{StaticResource Badge}">
    <Setter Property="Background" Value="{DynamicResource BrushConflictSoft}" />
  </Style>

  <Style x:Key="BadgeUnknown" TargetType="Border" BasedOn="{StaticResource Badge}">
    <Setter Property="Background" Value="{DynamicResource BrushUnknownSoft}" />
  </Style>

  <Style x:Key="ActionBase" TargetType="Button">
    <Setter Property="MinHeight" Value="38" />
    <Setter Property="Padding" Value="16,7" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="FontSize" Value="12.5" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" Background="{TemplateBinding Background}"
                  CornerRadius="4" Padding="{TemplateBinding Padding}"
                  BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                              TextElement.Foreground="{TemplateBinding Foreground}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Opacity" Value="0.88" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource BrushTextPrimary}" />
              <Setter TargetName="Bd" Property="BorderThickness" Value="2" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.42" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ActionPrimary" TargetType="Button" BasedOn="{StaticResource ActionBase}">
    <Setter Property="Background" Value="{DynamicResource BrushActionGradient}" />
    <Setter Property="Foreground" Value="White" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>

  <Style x:Key="ActionDanger" TargetType="Button" BasedOn="{StaticResource ActionBase}">
    <Setter Property="Background" Value="{DynamicResource BrushError}" />
    <Setter Property="Foreground" Value="White" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushError}" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>

  <Style x:Key="ActionSuccess" TargetType="Button" BasedOn="{StaticResource ActionBase}">
    <Setter Property="Background" Value="{DynamicResource BrushSuccess}" />
    <Setter Property="Foreground" Value="White" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushSuccess}" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>

  <Style x:Key="ActionNeutral" TargetType="Button" BasedOn="{StaticResource ActionBase}">
    <Setter Property="Background" Value="{DynamicResource BrushBgCard}" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushBorderStrong}" />
    <Setter Property="BorderThickness" Value="2" />
  </Style>

  <Style x:Key="SectionToggle" TargetType="Button" BasedOn="{StaticResource ActionNeutral}">
    <Setter Property="MinHeight" Value="42" />
    <Setter Property="Padding" Value="13,7" />
    <Setter Property="Margin" Value="0,0,8,0" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>

  <Style x:Key="SectionToggleSelected" TargetType="Button" BasedOn="{StaticResource SectionToggle}">
    <Setter Property="Background" Value="{DynamicResource BrushBgCard}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.28" Color="#000000" />
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ActionChoice" TargetType="Button" BasedOn="{StaticResource ActionNeutral}">
    <Setter Property="MinHeight" Value="38" />
    <Setter Property="MinWidth" Value="108" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="Margin" Value="0,0,8,7" />
  </Style>

  <Style x:Key="ActionChoiceSelected" TargetType="Button" BasedOn="{StaticResource ActionChoice}">
    <Setter Property="Background" Value="{DynamicResource BrushAccent}" />
    <Setter Property="Foreground" Value="White" />
    <Setter Property="BorderBrush" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="BorderThickness" Value="2" />
  </Style>

  <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionPrimary}" />
  <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionNeutral}" />
  <Style x:Key="OptionButton" TargetType="Button" BasedOn="{StaticResource ActionChoice}" />
  <Style x:Key="LinkButton" TargetType="Button" BasedOn="{StaticResource ActionNeutral}">
    <Setter Property="MinHeight" Value="30" />
    <Setter Property="Padding" Value="8,3" />
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="FontSize" Value="11" />
  </Style>

  <Style x:Key="DomainTile" TargetType="Button">
    <Setter Property="Background" Value="{DynamicResource BrushBgCard}" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="12,10" />
    <Setter Property="Margin" Value="0,0,8,8" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="MinWidth" Value="140" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4"
                  BorderBrush="{DynamicResource BrushBorderStrong}" BorderThickness="1"
                  Padding="{TemplateBinding Padding}">
            <Border BorderBrush="{DynamicResource BrushAccent}" BorderThickness="3,0,0,0"
                    Margin="-12,0,0,0" Padding="12,0,0,0">
              <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"
                                TextElement.Foreground="{TemplateBinding Foreground}" />
            </Border>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BrushBgHover}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="BreadcrumbText" TargetType="TextBlock">
    <Setter Property="FontSize" Value="12" />
    <Setter Property="Foreground" Value="{DynamicResource BrushTextMuted}" />
    <Setter Property="VerticalAlignment" Value="Center" />
  </Style>

  <Style x:Key="BreadcrumbLink" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="2,1" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="Foreground" Value="{DynamicResource BrushAccent}" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="Transparent" Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                              TextElement.Foreground="{TemplateBinding Foreground}" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="Expander">
    <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
    <Setter Property="Margin" Value="0,4,0,4" />
    <Setter Property="Padding" Value="0" />
  </Style>

  <Style x:Key="DetailExpander" TargetType="Expander" BasedOn="{StaticResource {x:Type Expander}}">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Margin" Value="0,6,0,0" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="FontWeight" Value="SemiBold" />
  </Style>

  <Style x:Key="RailExpander" TargetType="Expander" BasedOn="{StaticResource {x:Type Expander}}">
    <Setter Property="Foreground" Value="{DynamicResource BrushSidebarText}" />
    <Setter Property="FontSize" Value="10" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="Margin" Value="10,3,8,3" />
  </Style>

</ResourceDictionary>
