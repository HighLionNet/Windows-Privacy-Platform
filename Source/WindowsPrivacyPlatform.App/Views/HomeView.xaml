<UserControl x:Class="WindowsPrivacyPlatform.App.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel>
    <TextBlock Text="Machine Overview" Style="{StaticResource SectionTitle}" />
    <Border Style="{StaticResource HeaderRule}" />

    <Border x:Name="ConflictsCard" Style="{StaticResource CardConflict}" Visibility="Collapsed">
      <DockPanel LastChildFill="True">
        <StackPanel DockPanel.Dock="Left">
          <TextBlock Text="Attention" FontWeight="SemiBold" FontSize="12" />
          <TextBlock x:Name="ConflictsText" Style="{StaticResource ValueText}" Margin="0,3,0,0" />
        </StackPanel>
        <Button x:Name="OpenConflictsBtn" Content="Open Conflicts" Style="{StaticResource SecondaryButton}"
                HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="Collapsed" />
      </DockPanel>
    </Border>

    <TextBlock Text="Identity" Style="{StaticResource GroupHeader}" Margin="0,2,0,4" />
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Border Style="{StaticResource PropertyGroup}" Margin="0,0,4,0">
        <StackPanel>
          <TextBlock Text="Operating system" Style="{StaticResource LabelMuted}" />
          <TextBlock x:Name="OsText" Style="{StaticResource PrimaryValue}" Margin="0,3,0,0" />
          <TextBlock x:Name="ArchText" Style="{StaticResource MetaText}" Margin="0,2,0,0" />
        </StackPanel>
      </Border>
      <Border Grid.Column="1" Style="{StaticResource PropertyGroup}" Margin="2,0,2,0">
        <StackPanel>
          <TextBlock Text="Device" Style="{StaticResource LabelMuted}" />
          <TextBlock x:Name="DeviceText" Style="{StaticResource PrimaryValue}" Margin="0,3,0,0" />
          <TextBlock x:Name="CpuText" Style="{StaticResource MetaText}" Margin="0,2,0,0" />
          <TextBlock x:Name="MemText" Style="{StaticResource MetaText}" Margin="0,1,0,0" />
        </StackPanel>
      </Border>
      <Border Grid.Column="2" Style="{StaticResource PropertyGroup}" Margin="4,0,0,0">
        <StackPanel>
          <TextBlock Text="Environment" Style="{StaticResource LabelMuted}" />
          <TextBlock x:Name="DomainText" Style="{StaticResource ValueText}" Margin="0,3,0,0" />
        </StackPanel>
      </Border>
    </Grid>

    <Border Style="{StaticResource SectionRule}" />

    <TextBlock Text="Security posture" Style="{StaticResource GroupHeader}" Margin="0,0,0,4" />
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Border Style="{StaticResource PropertyGroup}" Margin="0,0,4,0">
        <StackPanel>
          <TextBlock Text="Platform" Style="{StaticResource LabelMuted}" />
          <TextBlock x:Name="SecureBootText" Style="{StaticResource ValueText}" Margin="0,4,0,0" />
          <TextBlock x:Name="TpmText" Style="{StaticResource ValueText}" Margin="0,2,0,0" />
          <TextBlock x:Name="BitLockerText" Style="{StaticResource ValueText}" Margin="0,2,0,0" />
        </StackPanel>
      </Border>
      <Border Grid.Column="1" Style="{StaticResource PropertyGroup}" Margin="4,0,0,0">
        <StackPanel>
          <TextBlock Text="Services" Style="{StaticResource LabelMuted}" />
          <TextBlock x:Name="FirewallText" Style="{StaticResource ValueText}" Margin="0,4,0,0" />
          <TextBlock x:Name="DefenderText" Style="{StaticResource ValueText}" Margin="0,2,0,0" />
        </StackPanel>
      </Border>
    </Grid>

    <Border Style="{StaticResource SectionRule}" />

    <TextBlock Text="Scan" Style="{StaticResource GroupHeader}" Margin="0,0,0,4" />
    <Border Style="{StaticResource PropertyGroup}">
      <TextBlock x:Name="ScanMetaText" Style="{StaticResource ValueText}" />
    </Border>

    <Border Style="{StaticResource SectionRule}" />

    <Expander Header="Identity collection notes" Style="{StaticResource DetailExpander}" IsExpanded="False">
      <Border Style="{StaticResource PropertyGroup}" Margin="0,4,0,0">
        <TextBlock x:Name="IdentityNotesText" Style="{StaticResource ValueText}"
                   Foreground="{StaticResource BrushTextSecondary}" />
      </Border>
    </Expander>

    <Expander Header="Observation summary" Style="{StaticResource DetailExpander}" IsExpanded="False">
      <Border Style="{StaticResource PropertyGroup}" Margin="0,4,0,0">
        <TextBlock x:Name="SummaryText" Style="{StaticResource ValueText}" />
      </Border>
    </Expander>

    <Border Style="{StaticResource SectionRule}" />

    <TextBlock Text="Domains" Style="{StaticResource GroupHeader}" Margin="0,0,0,4" />
    <WrapPanel x:Name="QuickNavPanel" />
  </StackPanel>
</UserControl>
