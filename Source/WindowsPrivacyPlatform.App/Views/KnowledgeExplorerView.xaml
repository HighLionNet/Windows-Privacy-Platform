<UserControl x:Class="WindowsPrivacyPlatform.App.Views.KnowledgeExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel>
    <TextBlock Text="Knowledge Explorer" Style="{StaticResource SectionTitle}" />
    <TextBlock Text="Browse the curated catalog independent of live observations. Click any entry for full explanation."
               Style="{StaticResource SectionSubtitle}" />
    <ItemsControl x:Name="List" />
  </StackPanel>
</UserControl>
