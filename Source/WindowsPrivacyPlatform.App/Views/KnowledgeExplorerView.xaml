<UserControl x:Class="WindowsPrivacyPlatform.App.Views.KnowledgeExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" HorizontalContentAlignment="Stretch">
  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <StackPanel>
      <TextBlock Text="Knowledge Explorer" Style="{StaticResource SectionTitle}"/>
      <TextBlock Text="Search the complete local catalog, including reference-only and unavailable controls." Style="{StaticResource SectionSubtitle}"/>
      <Border Style="{StaticResource HeaderRule}"/>
    </StackPanel>
    <Border Grid.Row="1" Style="{StaticResource ListContainer}" Padding="14,12" Margin="0,0,0,10" BorderThickness="4,1,1,1" BorderBrush="{DynamicResource BrushAccentCyan}">
      <DockPanel>
        <TextBlock x:Name="CountText" DockPanel.Dock="Right" Margin="14,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource BrushTextMuted}"/>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="Search:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,6,0"/>
          <TextBox x:Name="SearchBox" MinWidth="360" Height="34" VerticalContentAlignment="Center" TextChanged="SearchChanged" AutomationProperties.Name="Search catalog"/>
        </StackPanel>
      </DockPanel>
    </Border>
    <Grid Grid.Row="2" Style="{StaticResource ResultsDivider}"><Border Width="132" Background="{DynamicResource BrushAccentCyan}" HorizontalAlignment="Left"/></Grid>
    <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <StackPanel x:Name="ResultsPanel"/>
    </ScrollViewer>
  </Grid>
</UserControl>
