<UserControl x:Class="WindowsPrivacyPlatform.App.Views.ServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <StackPanel>
      <TextBlock Text="Services" Style="{StaticResource SectionTitle}"/>
      <TextBlock x:Name="SubtitleText" Style="{StaticResource SectionSubtitle}"/>
      <TextBlock Text="Read-only evidence. This page never starts, stops, enables, disables, or deletes a service."
                 Margin="0,5,0,0" TextWrapping="Wrap" Foreground="{DynamicResource BrushTextSecondary}"/>
      <Border Style="{StaticResource HeaderRule}"/>
    </StackPanel>
    <Border Grid.Row="1" Style="{StaticResource ListContainer}" Padding="12" Margin="0,0,0,10">
      <WrapPanel>
        <TextBlock Text="Search:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,6,8"/>
        <TextBox x:Name="SearchBox" Width="220" Height="34" Margin="0,0,14,8" VerticalContentAlignment="Center" TextChanged="FilterChanged"/>
        <TextBlock Text="State:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,6,8"/>
        <ComboBox x:Name="StateBox" Width="105" Height="34" Margin="0,0,12,8" SelectionChanged="FilterChanged">
          <ComboBoxItem Content="All" IsSelected="True"/><ComboBoxItem Content="Running"/><ComboBoxItem Content="Stopped"/><ComboBoxItem Content="Paused"/>
        </ComboBox>
        <TextBlock Text="Startup:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,6,8"/>
        <ComboBox x:Name="StartupBox" Width="115" Height="34" Margin="0,0,12,8" SelectionChanged="FilterChanged">
          <ComboBoxItem Content="All" IsSelected="True"/><ComboBoxItem Content="Automatic"/><ComboBoxItem Content="Manual"/><ComboBoxItem Content="Disabled"/>
        </ComboBox>
        <TextBlock Text="Publisher:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,6,8"/>
        <ComboBox x:Name="PublisherBox" Width="150" Height="34" Margin="0,0,12,8" SelectionChanged="FilterChanged" IsTextSearchEnabled="True">
          <ComboBoxItem Content="All" IsSelected="True"/><ComboBoxItem Content="Microsoft"/><ComboBoxItem Content="Non-Microsoft"/><ComboBoxItem Content="Unknown publisher"/>
        </ComboBox>
        <TextBlock Text="Issue:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,6,8"/>
        <ComboBox x:Name="IssueBox" Width="220" Height="34" Margin="0,0,0,8" SelectionChanged="FilterChanged" IsTextSearchEnabled="True">
          <ComboBoxItem Content="All" IsSelected="True"/><ComboBoxItem Content="No issue observed"/><ComboBoxItem Content="Automatic service is stopped"/>
          <ComboBoxItem Content="Executable missing or inaccessible"/><ComboBoxItem Content="Dependency issue observed"/>
          <ComboBoxItem Content="Configuration issue observed"/><ComboBoxItem Content="Access denied"/><ComboBoxItem Content="Unable to verify"/>
        </ComboBox>
      </WrapPanel>
    </Border>
    <Border Grid.Row="2" Style="{StaticResource ListContainer}" Margin="0">
      <ListBox x:Name="ServiceList" BorderThickness="0" Background="Transparent"
               ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto"
               VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
        <ListBox.ItemsPanel><ItemsPanelTemplate><VirtualizingStackPanel/></ItemsPanelTemplate></ListBox.ItemsPanel>
        <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0"/></Style></ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Border x:Name="RowBorder" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="0,0,0,1" Padding="12,9" Background="{DynamicResource BrushBgCard}">
              <StackPanel>
                <Grid>
                  <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="115"/><ColumnDefinition Width="130"/><ColumnDefinition Width="180"/></Grid.ColumnDefinitions>
                  <StackPanel Margin="0,0,14,0">
                    <TextBlock Text="{Binding DisplayName}" FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding Description}" Margin="0,2,0,0" FontSize="11" Foreground="{DynamicResource BrushTextSecondary}" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding Name}" Margin="0,3,0,0" FontSize="10.5" FontFamily="Cascadia Mono, Consolas" Foreground="{DynamicResource BrushTextMuted}"/>
                  </StackPanel>
                  <StackPanel Grid.Column="1"><TextBlock Text="STATE" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}"/><TextBlock Text="{Binding State}" Margin="0,3,0,0"/></StackPanel>
                  <StackPanel Grid.Column="2"><TextBlock Text="STARTUP" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}"/><TextBlock Text="{Binding Startup}" Margin="0,3,0,0" TextWrapping="Wrap"/></StackPanel>
                  <StackPanel Grid.Column="3"><TextBlock Text="ISSUE" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}"/><TextBlock Text="{Binding Issue}" Margin="0,3,0,0" TextWrapping="Wrap"/></StackPanel>
                </Grid>
                <Expander Header="Service details" Style="{StaticResource DetailExpander}" Margin="0,6,0,0">
                  <Grid Margin="0,4,0,2">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                    <TextBlock Text="Account" Style="{StaticResource PropLabel}"/><TextBlock Grid.Column="1" Text="{Binding Account}" Style="{StaticResource PropValue}"/>
                    <TextBlock Grid.Row="1" Text="Publisher" Style="{StaticResource PropLabel}"/><TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Publisher}" Style="{StaticResource PropValue}"/>
                    <TextBlock Grid.Row="2" Text="Signature evidence" Style="{StaticResource PropLabel}"/><TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Signature}" Style="{StaticResource PropValue}"/>
                    <TextBlock Grid.Row="3" Text="Executable" Style="{StaticResource PropLabel}"/><TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Executable}" Style="{StaticResource PropValue}" FontFamily="Cascadia Mono, Consolas"/>
                    <TextBlock Grid.Row="4" Text="Relationships" Style="{StaticResource PropLabel}"/><TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Relationships}" Style="{StaticResource PropValue}"/>
                  </Grid>
                </Expander>
              </StackPanel>
            </Border>
            <DataTemplate.Triggers><DataTrigger Binding="{Binding HasIssue}" Value="True"><Setter TargetName="RowBorder" Property="BorderBrush" Value="{DynamicResource BrushWarning}"/><Setter TargetName="RowBorder" Property="BorderThickness" Value="3,0,0,1"/></DataTrigger></DataTemplate.Triggers>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Border>
  </Grid>
</UserControl>
