<UserControl x:Class="WindowsPrivacyPlatform.App.Views.DomainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" HorizontalContentAlignment="Stretch">
  <StackPanel>
    <TextBlock x:Name="TitleText" Style="{StaticResource SectionTitle}" />
    <TextBlock x:Name="SubtitleText" Visibility="Collapsed" />
    <Border Style="{StaticResource HeaderRule}" />
    <Border x:Name="ProtectionProductPanel" Style="{StaticResource Card}" Visibility="Collapsed"
            BorderThickness="4,1,1,1" BorderBrush="{DynamicResource BrushDomainSecurity}" Margin="0,0,0,12">
      <StackPanel>
        <TextBlock Text="Protection product" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}"/>
        <TextBlock x:Name="ProtectionProductText" Margin="0,4,0,0" FontWeight="SemiBold" TextWrapping="Wrap"/>
      </StackPanel>
    </Border>
    <Border x:Name="EdgePresencePanel" Style="{StaticResource Card}" Visibility="Collapsed"
            BorderThickness="4,1,1,1" BorderBrush="{DynamicResource BrushDomainPrivacy}" Margin="0,0,0,12">
      <StackPanel>
        <TextBlock Text="EDGE PRESENCE &amp; HANDOFFS" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}"/>
        <TextBlock x:Name="EdgePresenceText" Margin="0,5,0,0" TextWrapping="Wrap"/>
        <TextBlock x:Name="WebViewPresenceText" Margin="0,4,0,0" TextWrapping="Wrap" Foreground="{DynamicResource BrushTextSecondary}"/>
        <TextBlock x:Name="DefaultBrowserText" Margin="0,4,0,0" TextWrapping="Wrap" Foreground="{DynamicResource BrushTextSecondary}"/>
        <TextBlock Text="WebView2 is a shared runtime, not the Edge browser, and is never a remove target." Margin="0,7,0,0" FontSize="11" Foreground="{DynamicResource BrushTextMuted}" TextWrapping="Wrap"/>
        <WrapPanel Margin="0,8,0,0">
          <Button Content="Windows Default apps" Style="{StaticResource ActionNeutral}" Margin="0,0,8,0" Click="DefaultApps_Click"/>
          <Button Content="Apps &amp; features" Style="{StaticResource ActionNeutral}" Click="AppsFeatures_Click"/>
        </WrapPanel>
      </StackPanel>
    </Border>
    <Border Style="{StaticResource ListContainer}">
      <StackPanel>
        <Border Style="{StaticResource ListHeader}">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="90" />
              <ColumnDefinition Width="90" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Category or setting" FontWeight="SemiBold" FontSize="11"
                       Foreground="{DynamicResource BrushTextSecondary}" />
            <TextBlock Grid.Column="1" Text="Settings" FontWeight="SemiBold" FontSize="11"
                       Foreground="{DynamicResource BrushTextSecondary}" HorizontalAlignment="Right" />
            <TextBlock Grid.Column="2" Text="Attention" FontWeight="SemiBold" FontSize="11"
                       Foreground="{DynamicResource BrushTextSecondary}" HorizontalAlignment="Right"
                       Margin="0,0,4,0" />
          </Grid>
        </Border>
        <StackPanel x:Name="CategoryList" />
      </StackPanel>
    </Border>
  </StackPanel>
</UserControl>
